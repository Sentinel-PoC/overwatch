# Overwatch Runbooks

Operator procedures and supporting artifacts for the overwatch platform.

## Netbox / XFS Migration

| File | Purpose |
|------|---------|
| [`netbox-pvc-render-path.md`](netbox-pvc-render-path.md) | Authoritative render path for `netbox-postgresql-data` PVC — multi-source ArgoCD Application breakdown, ignoreDifferences git blame, live vs Git diff explanation. Reference for OPS-94 (PV manifest location) and OPS-95 (PVC field changes). NIST: CM-3, CM-4, AU-6. |

## Break-Glass Recovery

| File | Purpose |
|------|---------|
| [`break-glass-proxmox-qga.md`](break-glass-proxmox-qga.md) | Step-by-step procedure for recovering VM access via Proxmox console + qemu-guest-agent when SSH/Vault are unavailable. Covers: restore sudoers, re-add SSH key, restart Vault, restart sshd. NIST: CP-10, AC-5, AU-6. |
| [`qga-audit-2026-04-20.md`](qga-audit-2026-04-20.md) | Live audit (2026-04-20) confirming qemu-guest-agent is installed and active on all critical cluster VMs. All 12 running critical VMs: qga active, guest-exec enabled. Generated by `scripts/audit_qga.py`. |

## SIEM / Wazuh

| File | Purpose |
|------|---------|
| [`wazuh-decoders/proxmox-qga-exec.xml`](wazuh-decoders/proxmox-qga-exec.xml) | Wazuh decoder + rules for alerting on Proxmox API calls matching `POST /nodes/*/qemu/*/agent/exec*`. Level 12 (high) on every break-glass exec call. Deploy to Wazuh manager per instructions in file. NIST: AU-6, AU-12. |

## Scripts

| Script | Purpose |
|--------|---------|
| [`../scripts/audit_qga.py`](../scripts/audit_qga.py) | Reproducible qga audit script. Read-only Proxmox API calls. Fetches token from Vault. Run: `python3 scripts/audit_qga.py`. |

## Issue Tracking

All runbooks in this directory originated from Plane issues. Do not modify without creating a Plane issue.

| Issue | Title |
|-------|-------|
| OPS-262 | Break-glass recovery: audit + document Proxmox console + qga path |
| OPS-93 | Document netbox PVC render path (OPS-81 audit trail) |
