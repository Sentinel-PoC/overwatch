# L1 — Physical Layer

> Auto-generated by `overwatch-gen`. Do not edit by hand.
> Add commentary in `notes.md` — it will not be overwritten.

## Overview

Physical infrastructure: Proxmox nodes, virtual machines, Unifi network devices.

**Proxmox nodes:** {{ nodes | length }}
**Virtual machines:** {{ vms | length }}
**Unifi devices:** {{ devices | length }}

## Tables

### Proxmox Nodes

| Node | Status | CPUs | RAM (GB) | CPU% |
|------|--------|------|----------|------|
{% for node in nodes -%}
| `{{ node.name }}` | {{ node.status }} | {{ node.maxcpu }} | {{ (node.maxmem / 1073741824) | round(1) }} | {{ (node.cpu_usage * 100) | round(1) }}% |
{% endfor %}

### Virtual Machines

| VMID | Name | Node | Type | Status | vCPUs | RAM (GB) |
|------|------|------|------|--------|-------|----------|
{% for vm in vms -%}
| {{ vm.vmid }} | `{{ vm.name }}` | {{ vm.node }} | {{ vm.type }} | {{ vm.status }} | {{ vm.cpus }} | {{ (vm.maxmem / 1073741824) | round(1) }} |
{% endfor %}

### Storage Pools

| Storage | Node | Type | Size (TB) | Available (TB) | Shared |
|---------|------|------|-----------|----------------|--------|
{% for s in storage -%}
| `{{ s.storage }}` | {{ s.node }} | {{ s.type }} | {{ (s.maxdisk / 1099511627776) | round(2) }} | {{ (s.avail / 1099511627776) | round(2) }} | {{ "yes" if s.shared else "no" }} |
{% endfor %}

### Unifi Devices

| Name | Type | Model | IP | Status |
|------|------|-------|----|--------|
{% for d in devices -%}
| `{{ d.name }}` | {{ d.type }} | {{ d.model }} | {{ d.ip }} | {{ d.state }} |
{% endfor %}

## Diagrams

- `physical-topology.d2` — D2 source: physical host layout
- `physical-topology.svg` — SVG export (rendered by CI)

## Anchors

<!-- stable anchors for cross-layer links — do not remove -->
<a id="overview"></a>
<a id="tables"></a>
<a id="diagrams"></a>
