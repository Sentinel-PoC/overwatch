# L3 — Network Layer

> Auto-generated by `overwatch-gen`. Do not edit by hand.
> Add commentary in `notes.md` — it will not be overwritten.

## Overview

Network layer: IP prefixes, address assignments, and firewall policies.

**Prefixes:** {{ prefixes | length }}
**IP Addresses:** {{ ip_addresses | length }}
**Firewall Policies:** {{ firewall_policies | length }}
**Firewall Zones:** {{ firewall_zones | length }}

## Tables

### IP Prefixes (NetBox)

| Prefix | Family | Status | Is Pool | VLAN | Description |
|--------|--------|--------|---------|------|-------------|
{% for p in prefixes -%}
| `{{ p.prefix }}` | IPv{{ p.family }} | {{ p.status }} | {{ "yes" if p.is_pool else "no" }} | {% if p.vlan %}{{ p.vlan }}{% else %}—{% endif %} | {{ p.description }} |
{% endfor %}

### IP Addresses (NetBox)

| Address | DNS Name | Status | Role | Description |
|---------|----------|--------|------|-------------|
{% for ip in ip_addresses -%}
| `{{ ip.address }}` | {{ ip.dns_name or "—" }} | {{ ip.status }} | {{ ip.role or "—" }} | {{ ip.description }} |
{% endfor %}

### Firewall Zones (Unifi)

| Zone | ID | Networks |
|------|----|----------|
{% for z in firewall_zones -%}
| {{ z.name }} | `{{ z.id }}` | {{ z.network_ids | join(", ") or "—" }} |
{% endfor %}

### Firewall Policies (Unifi)

| Name | Action | Enabled | Description |
|------|--------|---------|-------------|
{% for p in firewall_policies -%}
| `{{ p.name }}` | **{{ p.action }}** | {{ "yes" if p.enabled else "no" }} | {{ p.description }} |
{% endfor %}

## Diagrams

- `network-topology.d2` — D2 source: subnet and routing layout
- `network-topology.svg` — SVG export (rendered by CI)

## Anchors

<!-- stable anchors for cross-layer links — do not remove -->
<a id="overview"></a>
<a id="tables"></a>
<a id="diagrams"></a>
